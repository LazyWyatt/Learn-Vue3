let message1:string = 'hello';
let message2:string = 'world';

// 默認情況下，如果可以推斷出對應的標識符類型時， 一般可以不加。
const name = "wyatt";
const age = 23;
const height = 1.65;

let message3 = `name: ${name} age:${age} height: ${height}`

export {}